"use client";
import { FormEvent, useState } from "react";
export function AddressesManager({ initialAddresses }: { initialAddresses: any[] }) { const [addresses, setAddresses] = useState(initialAddresses); async function add(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const response = await fetch("/api/addresses", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget).entries())) }); if (response.ok) window.location.reload(); } async function remove(id: string) { await fetch("/api/addresses", { method: "DELETE", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id }) }); setAddresses(addresses.filter((address) => address.id !== id)); } return <div className="grid gap-6 lg:grid-cols-[1fr_1fr]"><section className="rounded-2xl bg-white p-6"><h2 className="font-display text-2xl font-bold text-navy">Saved addresses</h2><div className="mt-5 grid gap-3">{addresses.map((address: any) => <div key={address.id} className="rounded-xl border border-line p-4"><div className="flex justify-between"><p className="font-bold text-navy">{address.label}</p><button onClick={() => remove(address.id)} className="text-xs font-bold text-red-500">Remove</button></div><p className="mt-2 text-sm text-muted">{address.full_name} · {address.phone}<br />{address.address}, {address.area}, {address.city}</p></div>)}{!addresses.length && <p className="text-sm text-muted">No saved addresses yet.</p>}</div></section><form onSubmit={add} className="rounded-2xl bg-white p-6"><h2 className="font-display text-2xl font-bold text-navy">Add an address</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["label","full_name","phone","city","area","landmark"].map((name) => <input key={name} name={name} required={!['landmark'].includes(name)} placeholder={name.replaceAll("_", " ")} className="rounded-xl border border-line bg-cream px-3 py-3 text-sm" />)}</div><textarea name="address" required rows={3} placeholder="Complete address" className="mt-3 w-full rounded-xl border border-line bg-cream px-3 py-3 text-sm" /><textarea name="instructions" rows={2} placeholder="Delivery instructions" className="mt-3 w-full rounded-xl border border-line bg-cream px-3 py-3 text-sm" /><button className="mt-4 rounded-xl bg-orange px-5 py-3 text-sm font-bold text-white">Save address</button></form></div>; }
